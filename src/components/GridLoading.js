import React from 'react';

import classes from './GridLoading.module.scss';

const GridLoading = () => {

    return (
        <section className={classes.container}>
            <div>Loading...</div>
        </section>
    )
};

export default GridLoading;